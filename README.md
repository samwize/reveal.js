# README for slides.samwize.com

The slides uses [reveal.js](http://lab.hakim.se/reveal-js/) and is served on Github Pages.

## Setup

   npm install
   grunt serve
   
Open http://localhost:8000 

Or change the port by using `grunt serve --port 8001`.
